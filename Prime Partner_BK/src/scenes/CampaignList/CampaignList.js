import React from "react";
import { View, Text } from "react-native";

const CampaignList = () => {
  return (
    <View style={styles.container}>
      <Text>CampaignList</Text>
    </View>
  );
};

export default CampaignList;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
});
